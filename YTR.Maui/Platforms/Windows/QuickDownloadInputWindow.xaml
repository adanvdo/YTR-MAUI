<?xml version="1.0" encoding="utf-8"?>
<winui:Window
    x:Class="YTR.Maui.Platforms.Windows.QuickDownloadInputWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winui="using:Microsoft.UI.Xaml"
    Title="YTR - Quick Download">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
          Padding="24" RowSpacing="12" MinWidth="440">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with title and close button -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Quick Download" FontSize="18" FontWeight="SemiBold"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="CloseButton" Content="✕" Click="CloseButton_Click"
                    Background="Transparent" BorderThickness="0" FontSize="14"
                    Padding="8,4" VerticalAlignment="Center"/>
        </Grid>

        <!-- URL input and Download button -->
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="UrlTextBox" PlaceholderText="Paste URL here..."
                     KeyDown="UrlTextBox_KeyDown" VerticalAlignment="Center"/>
            <Button Grid.Column="1" x:Name="DownloadButton" Content="Download"
                    Click="DownloadButton_Click" Style="{ThemeResource AccentButtonStyle}"
                    VerticalAlignment="Center"/>
        </Grid>

        <!-- Disclaimer -->
        <TextBlock Grid.Row="2" TextWrapping="Wrap" FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="The best available format will be downloaded using your current settings (restrictions and conversions apply)."/>

        <!-- Status (hidden until download starts) -->
        <StackPanel Grid.Row="3" x:Name="StatusPanel" Visibility="Collapsed" Spacing="8">
            <TextBlock x:Name="StatusText" FontSize="13"/>
            <ProgressBar x:Name="ProgressBar" Minimum="0" Maximum="100"
                         Value="0" Height="6" IsIndeterminate="True"/>
        </StackPanel>
    </Grid>
</winui:Window>
